// los objetos son la pieza principal en javascript, en lugar de crar diferentes variables
const nombreproducto = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

// podemos crear un objeto que agrupe toda esta informacion en una sola variable

const producto1 = { // esto se le conoce como object literal
}

// estas llaves despues del signo = representan un objeto 

const producto2 = {
    nombre: jhonas} // esto se le llama una propiedad o llave del objeto
;

const producto3 = {
    nombre: "Monitor 20 pulgadas", // como en lugar del signo igual se utilizan :
}

// si deseas agregar mas propiedades puedes hacerlo de la siguiente forma
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}

const estudiante = {
    nombre: "Jhonas",
    edad: 16,
    curso: "Programacion",
    aprobado: false,
}

