// veamos como acceder a los elementos de un arreglo

const numeros= [10,20,30,40,50];

// esta vez utilizando

console.table(numeros);


// la forma en la que accedes a un arreglo es por lo que conoce como el indice en el arreglo, los arreglos inician en 0 usualmente

console.log(numeros[0])
console.log(numeros[1])
console.log(numeros[3])
console.log(numeros[20]) // que pasa si mando a imprimir una posicion que no existe que me arroja la consola

